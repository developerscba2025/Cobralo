/**
 * Configuration for the Landing components within the main app.
 * Since they are now in the same project, we can use relative routes.
 */
export const APP_URL = 'https://www.cobralo.info';
export const LANDING_URL = 'https://www.cobralo.info';
export const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:3000/api';
